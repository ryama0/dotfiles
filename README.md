# dotfiles
Configurations of UNIX Application

## setup
```bash
./install.sh
```

for uninstall
```bash
./uninstall.sh
```

### alacritty
```bash
ln -s ${REPOSITORY_ROOT}/alacritty/${OS_NAME}.toml ~/.alacritty.toml
```